# Action 2 — Réaliser le travail demandé

> Fait partie du protocole en 8 actions (section 3 de `../../INIT_ETAPES.md`), à
> appliquer identiquement pour chaque étape de `../MAIN.md`. Ce fichier documente le
> protocole lui-même, pas une instance pour une étape donnée.

## Description

Implémenter le travail décrit dans le fichier `steps/{id}_{slug}_{status}.md` de l'étape en cours, dans les limites de son périmètre.

## Notes

Respecter les principes d'architecture de la section 4 de `INIT_ETAPES.md` : réutiliser l'admin existant (`cms/assets/adm`), découpage par plugin sauf mention « pas un plugin », `cms/src/*` réservé au code réellement transverse, gestion des fichiers centralisée, multilangue partout.
