# Action 7 — Commit puis push

> Fait partie du protocole en 8 actions (section 3 de `../../INIT_ETAPES.md`), à
> appliquer identiquement pour chaque étape de `../MAIN.md`. Ce fichier documente le
> protocole lui-même, pas une instance pour une étape donnée.

## Description

Committer les changements de l'étape (code + commentaires + tests + bump de version + entrée RELEASE.md) puis pousser vers le remote.

## Notes

Suivre le protocole git standard du projet (voir consignes générales de commit). Un commit par étape, sauf nécessité contraire.
